<%= form_with(model: activities_table) do |form| %>
  <% if activities_table.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(activities_table.errors.count, "error") %> prohibited this activities_table from being saved:</h2>

      <ul>
        <% activities_table.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :ordered, style: "display: block" %>
    <%= form.datetime_field :ordered %>
  </div>

  <div>
    <%= form.label :complete, style: "display: block" %>
    <%= form.check_box :complete %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
