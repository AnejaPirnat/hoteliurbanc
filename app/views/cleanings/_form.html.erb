<%= form_with(model: cleaning) do |form| %>
  <% if cleaning.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cleaning.errors.count, "error") %> prohibited this cleaning from being saved:</h2>

      <ul>
        <% cleaning.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :hour, style: "display: block" %>
    <%= form.time_field :hour %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
